version: "3.5"
services:
  app:
    image: ghcr.io/ddbj/yevis-cli:0.3.0
    container_name: yevis-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - SAPPORO_RUN_DIR=${PWD}/sapporo_run
    entrypoint: [ "" ]
    command: [ "sleep", "infinity" ]
    restart: on-failure
    working_dir: /app
    networks:
      - gh-trs-network

networks:
  gh-trs-network:
    name: gh-trs-network
